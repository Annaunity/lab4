package aboba;
public interface ZnaikasAct {
    String sleep(Human znaika);
    String wrapped(Human znaika);
    String wake(Human znaika);
    String landing(Human znaika);
    String search(Human znaika);
}
